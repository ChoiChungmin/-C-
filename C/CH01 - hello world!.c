#include <stdio.h> //헤더파일 선언문. stdio.h 파일의 내용을 이 위치에 가져다 놓아라.

int main (void) //int = 출력형태, main = 함수이름, void = 입력형태
{
    printf("Hello Wolrd! \n"); //함수내에 존재하는 문장의 끝에는 ;을 붙인다
    return 0; //함수를 호출한 영역으로 값을 반환, 현재 실행중인 함수의 종료
}